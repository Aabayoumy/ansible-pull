---  
- name: neovim
  unarchive:
    remote_src: yes
    src: "https://github.com/neovim/neovim/releases/download/stable/nvim-linux64.tar.gz"
    dest: "files/nvim-linux64.tar.gz"
    keep_newer: yes
    extra_opts:
      - --strip=1
      - --no-anchored